    <user name="<%= @username %>" password="<%= @password %>">
        <attribute name="fedoraRole">
            <% @roles.each do |role| -%>                <value><%= role %></value>
<% end -%>
        </attribute>
    </user>
